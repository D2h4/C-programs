#include<stdio.h>
#include<conio.h>
void main()
{   
    int n,i=0;
    printf("enter the number");
    scanf("%d",&n);
    while(i<=2*n)
    {
        printf("%d",i);
        i=i+2; 
    }
    getch();
}
