#include<stdio.h>

int main()
{
    int n,i=1;
    printf("enter the value of n: ");
    scanf("%d",&n);
    while(i<=n*7)
    {
       if(i%7==0)
       {
        
         printf("%d",i);
       }
       i++;
    }
    return 0;
}
