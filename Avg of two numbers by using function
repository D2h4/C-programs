//Avg of two numbers by using function
#include<stdio.h>
int avg(int a,int b);//function prototyping
int main()
{
    int a=3,b=6;
    int n=avg(a,b);//function calling
    printf("%d",n);
    return 0;
}
//call by value
int avg(int a,int b)//function defination
{  return (a+b)/2;
}
