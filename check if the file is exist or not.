//check if the file is exist or not.
#include<stdio.h>
int main()
{
    FILE *fptr;
    fptr=fopen("text.txt","r");
    if(fptr==NULL)
    {
        printf("file does not exist ");

    }
    else{
        fclose(fptr);
    }
    return 0;
}
